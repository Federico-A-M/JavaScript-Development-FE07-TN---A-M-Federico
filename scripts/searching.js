// const valores = Object.values(data.events);
//const $main_e = document.getElementById("main_index")
//tengo que editar el contenido del main no editarlo, borrarlo o filtrarlo


//let fragment = document.createDocumentFragment();
/*
document.getElementById("btn_searching").addEventListener("click", () => { 

    const $str_key = document.getElementById("name").value;
    alert($str_key)

});
*/














/*
const docu = document.;

export default function searchFilters(input, selector){
     docu.addEventListener("keyup", (e) => {
         if (e.target.matches(input)) {
             //console.log(e.key);
             console.log(e.target.value);
             docu.querySelectorAll(selector).forEach(el => (el.textContent.toLowerCase().includes(e.target.value))? el.class
             })




         }
     });
 }
 */